# The function takes the parent node from markdown_to_html
# identifies all the img tags 
# and adds a base path in-front of it
# what is does it returns the parent node with the image intact.
# we can also just modify the image node, let's check that out first
